'use strict';

var _typeof = typeof Symbol === "function" && typeof Symbol.iterator === "symbol" ? function (obj) { return typeof obj; } : function (obj) { return obj && typeof Symbol === "function" && obj.constructor === Symbol && obj !== Symbol.prototype ? "symbol" : typeof obj; }; /**
                                                                                                                                                                                                                                                                               * Test injectors
                                                                                                                                                                                                                                                                               */

var _index = require('../index');

var _index2 = _interopRequireDefault(_index);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

describe('index exports', function () {
  it('should have sagaInjector', function () {
    expect(typeof _index2.default === 'undefined' ? 'undefined' : _typeof(_index2.default)).toBe('function');
  });
  it('should have checkStore', function () {
    expect(typeof _index.checkStore === 'undefined' ? 'undefined' : _typeof(_index.checkStore)).toBe('function');
  });
});