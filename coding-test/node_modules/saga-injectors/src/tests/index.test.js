/**
 * Test injectors
 */

import sagaInjector, { checkStore } from '../index';

describe('index exports', () => {
  it('should have sagaInjector', () => {
    expect(typeof sagaInjector).toBe('function');
  });
  it('should have checkStore', () => {
    expect(typeof checkStore).toBe('function');
  });
});
